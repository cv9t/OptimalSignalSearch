#include <iostream>
//#include <chrono>
//#include <iomanip>
//#include "OptimalSignal.h"
//#include "MathUtils.h"

int main()
{
	/*auto start = std::chrono::high_resolution_clock::now();

	OptimalSignal* signal = new OptimalSignal(20);

	auto stop = std::chrono::high_resolution_clock::now();

	double duration = std::chrono::duration_cast<std::chrono::milliseconds>(stop - start).count() / 1000.0;

	 std::cout << *signal;

	std::cout << "Time: " << std::fixed << std::setprecision(2) << duration << " sec" << std::endl;*/

	//std::vector<int> sig{ -1, - 1, - 1, 1, 1, - 1 };

	//std::vector<int> acf = MathUtils::calcACF(sig);

	//std::cout << std::endl;
	//for (int i = 0; i < acf.size(); i += 1) {
	//	std::cout << acf[i] << " ";
	//}

	return 0;
}